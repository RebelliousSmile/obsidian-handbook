# Zombiology presentation handoff

The unfinished PJ, PNJ and monster presentation work is saved on [`feat/zombiology-presentation-preview`](https://github.com/RebelliousSmile/obsidian-handbook/tree/feat/zombiology-presentation-preview), not merged into `main`. Its [handoff note](https://github.com/RebelliousSmile/obsidian-handbook/blob/feat/zombiology-presentation-preview/aidd_docs/tasks/2026_09/2026_09_25_zombiology-preview/handoff.md) records tests and next steps.

The provider contract and checkpoint are on `schema-adrenaline/main` at `4296d3f`. Its new `./presentation` export is not yet in the published archive pinned by Handbook. The draft plugin also failed to load twice in Obsidian; the user's installed plugin was restored. A separate issue will plan that fix. Do not merge the preview branch into Handbook `main` or reinstall it in the main vault until the provider is released and the load failure is resolved.

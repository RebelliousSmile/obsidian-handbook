import { BlockShape } from "../blocks/shape";

export const rollerShape: BlockShape = {
	block: "roller",
	root: "brumes-roller",
	zones: [{ name: "table", holds: "the roller's Markdown table" }],
};
